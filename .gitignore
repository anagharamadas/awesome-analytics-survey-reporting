__pycache__/
*.py[cod]
.pytest_cache/
node_modules/
dist/
.env
.venv/
*.tsbuildinfo

# macOS
.DS_Store

# Assessment material - the company's IP, kept local, never published
Candidate Brief.html

# Local working notes, not for the public repo
PROCESS_LOG.md

# A bare (non-Docker) ingest run writes here; the committed copy is at the repo root
backend/rejects.csv
